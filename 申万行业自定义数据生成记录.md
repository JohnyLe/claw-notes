# 从本地 MySQL 生成通达信「申万行业」自定义数据文件

> 记录于 2026-09-06 ｜ 来源：本地 MySQL（wind 库）只读消费 ｜ 可信度：A（已交叉验证）

## 一、背景

想在通达信里加载「申万行业」自定义数据（文本型外部数据），用于个股的板块 / 行业标注。手头有一份第三方整理样表（小红牛版 `2025-10-31申万行业.txt`），核心问题是：**能否用本地已清洗的 MySQL 数据自己生成一份**，既摆脱对第三方样表的依赖，又保证口径可控。

结论：能，且沪深全覆盖、与样表交叉验证 100% 一致。

## 二、数据源

- 本地 MySQL 8.0.29 免安装版，运行于 `127.0.0.1:3306`，业务库 `wind`。
- 连接账号：用**只读 `ro_agent`**（符合"wind 库只做消费型只读"铁律），root 写权限一概未碰。
- 关键表：
  - `wind.stock_sw_mapping`（5169 行）：字段 `ts_code / name / l1_name / l2_name / l3_name / src`，其中 `src = wind_basicinfo`。
  - `wind.sw_industry`（444 行）：申万分类树。

## 三、样表格式破解（关键）

通达信文本型自定义数据，每支证券占**两行**：

```
{market}|{code}|  {行业名}
|0.000
```

- **market 映射**：`.SZ → 0`（深）/ `.SH → 1`（沪）/ `.BJ → 2`（北交所）。
  验证：样表 `300xxx → 0`、`688xxx → 1`、`920xxx → 2` 完全吻合。
- **行业名** = `l2_name -- l3_name`，并做两处清洗：
  1. 脱去 `(申万)` 后缀；
  2. 罗马数字 `Ⅱ → 2`、`Ⅲ → 3`。
- 验证：DB `IT服务Ⅱ(申万)--IT服务Ⅲ(申万)` → 样表 `IT服务2--IT服务3` ✓

## 四、成品

- 输出：`output/申万行业_本地MySQL_20260906.txt`
- 5169 支股票 × 2 行 = **10338 行**，GBK 编码（通达信传统格式，回读无乱码）。
- 交叉验证样表 6 个沪深代码（`300071 / 688095 / 301231 / 688336 / 300204 / 301292`）**100% 一致**。

## 五、⚠️ 认知更正（重要）

申万行业指数成分**本就不涵盖北交所个股**（申万宏源行业指数成分股为沪深 A 股，北交所在其列之外）。

因此：
- 成品不含北交所，在「申万行业指数」口径下是**正确行为、非缺陷**；
- 样表中的 `920xxx → market=2` 系第三方（小红牛整理版）**手工补录**，非申万官方指数口径；
- → 凡涉申万行业数据，**默认不补北交所**，避免画蛇添足。

## 六、铁律遵守

- 只读消费 MySQL，未写入任何业务库；
- 输出仅落 `output/`，未触碰通达信 `D:\zd_huatai\` 程序目录（只读取，零写入）。
